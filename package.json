{
  "name": "crosspost-plugin",
  "version": "1.0.0",
  "description": "Crosspost plugin for posting content across multiple social media platforms",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "module",
  "peerDependencies": {
    "every-plugin": "0.1.5",
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "@module-federation/enhanced": "^0.18.3",
    "@module-federation/node": "^2.7.15",
    "@module-federation/runtime-core": "^0.18.3",
    "@rspack/cli": "^1.5.2",
    "@rspack/core": "^1.5.2",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.19.19",
    "every-plugin": "0.1.5",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "build": "rspack build && tsc -p tsconfig.build.json",
    "dev": "rspack serve",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "demo": "node -r ts-node/register examples/comprehensive-demo.ts",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  }
}
